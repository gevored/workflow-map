{
  "name": "workflow-map",
  "version": "1.0.0",
  "description": "Mapa interativo de workflows n8n — Lead Capture",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
